# Air Ace 部署到 GitHub Pages（约 3 分钟）

## 第 0 步：创建 GitHub 仓库（网页操作，约 30 秒）

1. 打开 https://github.com/new
2. Repository name 填：`air-ace`
3. 选 **Public**，其余全部保持默认（不要勾选 README / .gitignore / license）
4. 点 **Create repository**

## 第 1 步：推送代码（本目录下运行）

```powershell
cd F:\DShaness\GLM\p_work\airace-site
.\deploy.ps1
```

首次推送会弹出 GitHub 登录窗口（Git Credential Manager），用浏览器授权一次即可。
若提示要求登录凭据失败，可改用 PAT：https://github.com/settings/tokens 生成 classic token（勾选 repo 权限），推送时用户名填 DDPAT987、密码填 token。

## 第 2 步：开启 Pages（网页操作，约 30 秒）

1. 打开 https://github.com/DDPAT987/air-ace/settings/pages
2. Source 选 **Deploy from a branch** → Branch 选 `main` / `(root)` → **Save**
3. 等待 1~3 分钟，顶部会显示绿色公示：

   `Your site is live at https://ddpat987.github.io/air-ace/`

## 第 3 步：验证

浏览器打开 https://ddpat987.github.io/air-ace/ ，看到「AIR ACE 苍穹王牌」主菜单即成功。
（首次加载约 10~40 秒——需要下载 60MB 高精度战机模型，页面有进度条；之后有浏览器缓存）

## 常见问题

- **推送 66MB 较慢**：正常，模型文件大，耐心等待；失败可重跑 deploy.ps1（会跳过已推送对象）
- **Pages 404**：确认 Settings → Pages 的 Branch 已保存为 main/(root)，且 Actions 部署完成
- **改了内容要更新**：改完文件后重跑 deploy.ps1 即可
